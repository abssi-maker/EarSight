# EARSIGHT — Agent Context (Plan mode)

Use Plan mode to:
- Review and revise `plans/00-build-plan.md`
- Produce step plans for individual steps before implementation
- Think through architecture decisions before writing code
- Answer questions without touching files

Do NOT implement in Plan mode. Switch to Agent mode for implementation.

## Plan file location

`plans/00-build-plan.md` — the canonical build plan.
Reference it with `@plans/00-build-plan.md` rather than restating its contents.
