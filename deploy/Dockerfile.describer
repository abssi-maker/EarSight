FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY agents/ agents/
COPY pyproject.toml .

ENV PYTHONPATH=/app
EXPOSE 8080
CMD ["uvicorn", "agents.describer.app:app", "--host", "0.0.0.0", "--port", "8080"]
